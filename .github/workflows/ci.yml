name: CI
on:
  push:
    branches-ignore:
      - main

jobs:

  verify:
    name: Verify
    runs-on: ubuntu-latest

    steps:

      - name: Check out code
        uses: actions/checkout@v3.1.0

      - name: Set up Ruby
        uses: ruby/setup-ruby@v1
        with:
          ruby-version: 2.7
          bundler: none

      - name: Install bundler
        run: gem install bundler -v 2.1.4

      - name: Try to create lockfile
        run: bundle _2.1.4_ lock
